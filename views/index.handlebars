<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand font-weight-bold" href="/">Space.com Scrape</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="/saved"> Saved Articles <span class="sr-only">(current)</span></a>
      <a class="nav-item nav-link" href="/articles"> Articles API </a>
      <a class="nav-item nav-link" href="/scrape"> New Scrape </a>
    </div>
  </div>
</nav>

<div class="jumbotron jumbotron-fluid">
  <div class="container text-center">
    <h1 class="display-4 font-weight-bold">Science & Tech News From Space.com!</h1>
  </div>
</div>

<div id="article">
<ul>
  {{#each articles}}
      <a href="{{link}}" target="_blank"><h3>{{title}}</h3></a>
      {{!-- <h3>{{title}}</h3>
      <h3>{{link}}</h3> --}}
      <h4>{{summary}}</h4>
      <button class="btn btn-primary btn-lg" type="submit" data-id="{{_id}}">Save Article</button>
      <hr>
  {{/each}}
</ul>
</div>

<div id="notes"></div>